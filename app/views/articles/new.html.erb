<div class="container">
  <h1>New article</h1>
  <%= form_with model: @article do |t| %>
    <%= t.label :title %>
    <%= t.text_field :title %>
    <%= t.label :content %>
    <%= t.text_field :content %>
    <%= t.submit %>
  <% end %>
  <%= link_to 'See all articles', articles_path %>
</div>
